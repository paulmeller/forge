/**
 * Shared visual constants for the Software Factory (`/factory`). The page is
 * intentionally dark-only (a command-center aesthetic), so colours are fixed
 * rather than themed via CSS variables.
 */
export const FACTORY_ACCENT = '#e8843f';
export const FACTORY_ACCENT_DIM = '#9a8f63';
